import Link from "next/link";import {ChevronLeft,Download,Library,PanelLeft,Plus,Users} from "lucide-react";import {addChapter} from "@/lib/actions";
export function BookNav({book,chapters,active}:{book:{id:string,title:string},chapters:{id:string,title:string;position:number}[],active?:string}){return <aside className="flex w-72 shrink-0 flex-col border-r border-black/8 bg-[#f3f3ef]"><div className="border-b border-black/8 p-4"><Link href="/" className="mb-4 flex items-center gap-2 text-xs font-medium text-black/45 hover:text-black"><ChevronLeft size={15}/>Library</Link><h2 className="truncate font-semibold tracking-[-.02em]">{book.title}</h2></div><nav className="border-b border-black/8 p-3"><Link className="workspace-link" href={`/books/${book.id}`}><PanelLeft/>Outline</Link><Link className="workspace-link" href={`/books/${book.id}/story-bible`}><Users/>Story bible</Link><Link className="workspace-link" href={`/books/${book.id}/publishing`}><Download/>Publish & export</Link></nav><div className="flex items-center justify-between px-4 pb-2 pt-4"><span className="text-[11px] font-bold uppercase tracking-widest text-black/35">Manuscript</span><form action={addChapter.bind(null,book.id)}><button aria-label="Add chapter"><Plus size={16}/></button></form></div><div className="flex-1 overflow-y-auto px-2 pb-4">{chapters.map(c=><Link key={c.id} href={`/books/${book.id}/chapters/${c.id}`} className={`chapter-link ${active===c.id?"active":""}`}><span>{String(c.position).padStart(2,"0")}</span><p>{c.title}</p></Link>)}</div><div className="border-t border-black/8 p-3"><Link className="workspace-link" href="/"><Library/>All books</Link></div></aside>}
