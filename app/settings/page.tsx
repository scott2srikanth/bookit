import {AppShell} from "@/components/app-shell";import {Cpu,Database,KeyRound,ShieldCheck} from "lucide-react";
export default function Settings(){return <AppShell><main className="mx-auto max-w-4xl px-6 py-12"><h1 className="text-4xl font-semibold tracking-[-.05em]">Settings</h1><p className="mt-3 text-black/50">BookIt is configured for a private, single-author workspace.</p><div className="mt-9 grid gap-5"><Card icon={<Database/>} title="Local database" text="Books, chapters, versions, and story notes are stored in data/bookit.db with WAL journaling and foreign-key protection." status="Connected"/><Card icon={<ShieldCheck/>} title="Privacy" text="No analytics, remote authentication, or manuscript uploads. Nothing leaves your computer unless you choose AI generation." status="Local only"/><Card icon={<KeyRound/>} title="AI provider" text="Add OPENAI_API_KEY to .env.local to enable chapter drafting. The key is read server-side and never sent to the browser." status={process.env.OPENAI_API_KEY?"Ready":"Optional"}/><Card icon={<Cpu/>} title="Model" text="The writing assistant uses gpt-5-mini by default for fast, high-quality chapter drafting." status={process.env.OPENAI_API_KEY?"gpt-5-mini":"Not enabled"}/></div></main></AppShell>}function Card({icon,title,text,status}:{icon:React.ReactNode,title:string,text:string,status:string}){return <div className="panel flex gap-5 p-6"><span className="grid size-11 shrink-0 place-items-center rounded-2xl bg-[#d9f45f] [&_svg]:size-5">{icon}</span><div><h2 className="font-semibold">{title}</h2><p className="mt-1 max-w-2xl text-sm leading-6 text-black/50">{text}</p></div><span className="ml-auto h-fit rounded-full bg-black/[.05] px-3 py-1 text-xs font-semibold">{status}</span></div>}
