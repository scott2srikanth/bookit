import {db} from "@/lib/db";import {chapters} from "@/lib/db/schema";import {and,eq} from "drizzle-orm";import {notFound} from "next/navigation";import {Editor} from "@/components/editor";
export default async function ChapterPage({params}:{params:Promise<{bookId:string,chapterId:string}>}){const{bookId,chapterId}=await params;const chapter=(await db.select().from(chapters).where(and(eq(chapters.id,chapterId),eq(chapters.bookId,bookId))))[0];if(!chapter)notFound();return <div className="flex min-h-full flex-col bg-white"><div className="border-b border-black/8 px-4 py-3 sm:px-6 sm:py-4"><p className="text-[10px] font-bold uppercase tracking-widest text-black/35">Chapter {chapter.position}</p><h1 className="mt-1 truncate text-lg font-semibold tracking-[-.03em] sm:text-xl">{chapter.title}</h1></div><div className="min-h-0 flex-1"><Editor chapterId={chapter.id} initialContent={chapter.content} summary={chapter.summary}/></div></div>}
