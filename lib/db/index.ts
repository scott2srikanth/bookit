import Database from "better-sqlite3";import {drizzle} from "drizzle-orm/better-sqlite3";import path from "node:path";import fs from "node:fs";import * as schema from "./schema";
const dataDir=path.join(process.cwd(),"data");fs.mkdirSync(dataDir,{recursive:true,mode:0o700});const sqlite=new Database(path.join(dataDir,"bookit.db"));sqlite.pragma("journal_mode = WAL");sqlite.pragma("foreign_keys = ON");sqlite.pragma("busy_timeout = 5000");
sqlite.exec(`CREATE TABLE IF NOT EXISTS books (id TEXT PRIMARY KEY,title TEXT NOT NULL,subtitle TEXT NOT NULL DEFAULT '',premise TEXT NOT NULL DEFAULT '',genre TEXT NOT NULL DEFAULT 'Fiction',audience TEXT NOT NULL DEFAULT 'Adult',tone TEXT NOT NULL DEFAULT 'Engaging',language TEXT NOT NULL DEFAULT 'English',status TEXT NOT NULL DEFAULT 'draft',description TEXT NOT NULL DEFAULT '',keywords TEXT NOT NULL DEFAULT '[]',created_at INTEGER NOT NULL,updated_at INTEGER NOT NULL);
CREATE TABLE IF NOT EXISTS chapters (id TEXT PRIMARY KEY,book_id TEXT NOT NULL REFERENCES books(id) ON DELETE CASCADE,position INTEGER NOT NULL,title TEXT NOT NULL,summary TEXT NOT NULL DEFAULT '',content TEXT NOT NULL DEFAULT '',status TEXT NOT NULL DEFAULT 'planned',created_at INTEGER NOT NULL,updated_at INTEGER NOT NULL);
CREATE TABLE IF NOT EXISTS chapter_versions (id TEXT PRIMARY KEY,chapter_id TEXT NOT NULL REFERENCES chapters(id) ON DELETE CASCADE,content TEXT NOT NULL,created_at INTEGER NOT NULL);
CREATE TABLE IF NOT EXISTS story_items (id TEXT PRIMARY KEY,book_id TEXT NOT NULL REFERENCES books(id) ON DELETE CASCADE,type TEXT NOT NULL,name TEXT NOT NULL,description TEXT NOT NULL DEFAULT '',created_at INTEGER NOT NULL,updated_at INTEGER NOT NULL);
CREATE INDEX IF NOT EXISTS idx_chapters_book_position ON chapters(book_id,position);CREATE INDEX IF NOT EXISTS idx_story_items_book_type ON story_items(book_id,type);CREATE INDEX IF NOT EXISTS idx_versions_chapter_created ON chapter_versions(chapter_id,created_at);`);
export const db=drizzle(sqlite,{schema});
